# Node.js
# This is a standard .gitignore file for a Node.js backend project.

# Dependency directories
# Node.js dependencies are installed in this directory, and we don't want to
# commit them to our repository.
/node_modules

# Environment variables
# It's crucial to ignore environment files that contain sensitive information
# like database passwords, API keys, or secret tokens.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build/distributable folders
# The `dist` folder is typically where the transpiled JavaScript (from TypeScript, etc.) or
# compiled assets are placed. These are generated files and should not be
# committed to Git.
/dist
/build
/lib
/generated

# Log files
# Ignore various log files generated by the application or a test runner.
npm-debug.log*
yarn-debug.log*
/log
*.log

# Operating System and IDE-specific files
# These are files created by different operating systems or integrated
# development environments (IDEs) and are not part of the project itself.
# They should not be committed.
.DS_Store          # macOS specific
.idea              # JetBrains IDEs (e.g., WebStorm, IntelliJ)
.vscode            # VS Code
*.swp              # Vim swap files

# Test and coverage directories
# Ignore the output of test runs and code coverage reports.
/coverage
/.nyc_output

# Temporary files and process IDs
# Files that are temporary or hold process IDs should be ignored.
/tmp
/pids
*.pid

# Nodemon (a common development tool)
# Nodemon can create a JSON file to track its state, which should be ignored.
.nodemon.json